kind: Service
apiVersion: v1
metadata:
  name: nl-ambulance-webapi
spec:
  selector:
    pod: nl-ambulance-webapi-label
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: webapi-port